# DataCenter 系統更新日誌 (Update Log)
**日期：2026-04-27**

本文件記錄了今日針對 DataCenter 專案進行的核心功能開發與前端視覺升級，主要分為「AI 與 ESG 指標連動」以及「80 吋展場大螢幕全域響應式優化」兩大階段。

---

## 階段一：AI 算力與 ESG 綠能指標全域連動

為了符合現代化 AI 基礎設施的管理需求，我們將伺服器節點的 AI 與 ESG 參數擴充至所有戰情儀表板，達成真正的 AI-Driven DCIM。

1. **核心資料模型擴充 (`store/useDcimStore.ts`)**
   - 於 `ServerData` 介面新增 `powerCap` (能耗上限)、`flops` (算力)、`carbonEmission` (碳排) 等屬性，確保資料能持久化。

2. **狀態總覽面板 (`app/page.tsx`)**
   - 新增全廠總算力統計面板：**TOTAL AI COMPUTE (PFLOPS)**，即時加總全系統算力。
   - 伺服器矩陣 (Server Matrix) 卡片新增綠色「GPU 硬體微型標籤（例如：8x NVIDIA HGX H100）」，落實硬體防呆與快速識別。

3. **廠務監控面板 (`app/facility/page.tsx`)**
   - 頂端基礎設施 KPI 儀表板新增 **CARBON (碳排放量)** 指標，自動彙整機房內所有設備的 `CO2e` 累積數值。

4. **設備控制面板 (`app/control/page.tsx`)**
   - 針對單一主機操作卡片加入 **GPU 型號標籤**，防止人員對高價值 AI 算力節點誤操作。
   - 實作了 **POWER CAP LIMIT (能耗上限)** 範圍滑桿，直接連動底層資料庫，為未來與實體設備 (IPMI/Redfish) API 對接做好準備。

---

## 階段二：80 吋展場電視「全螢幕響應式」視覺升級

因應電腦展展示需求，全面解除各面板的寬度限制，升級為可適應 4K/8K 超高解析度的流體網格系統 (Fluid Grid System)，展現大數據戰情室的氣勢。

1. **解除寬度限制與網格重構 (共 8 個頁面)**
   - 移除了原有的 `max-w-7xl` (1280px) 限制，改採 `w-full`。
   - 針對大螢幕加入了 `xl`, `2xl`, `min-[2560px]`, `min-[3840px]` 等超高解析度斷點。單排卡片數量可無限擴展至 6~8 張以上，完美消弭畫面兩側留白。
   - **影響頁面**：設備控制、AI 趨勢分析、維護排程、系統備份與復原、網路通訊設定、系統日誌與告警、工程覆寫模式、系統設定。

2. **展場專用巨量資料分頁優化**
   - **設備控制 (`app/control/page.tsx`)**：
     - 單頁顯示數量由 6 台巨幅提升至 **18 台**（3排 x 6張）。
     - *效能評估*：一般 DOM 元素渲染極快，不影響系統操作體驗。
   - **AI 趨勢分析 (`app/analysis/page.tsx`)**：
     - 單頁顯示數量由 6 組微調至 **12 組**。
     - *效能評估與優化*：為避免大量 Recharts SVG 與 ResizeObserver 在大螢幕造成主執行緒 (Main Thread) 渲染卡頓，設定在 12 組以取得「戰情氣勢」與「操作順暢度」的最佳平衡。

---
*Generated by Antigravity AI Assistant*
